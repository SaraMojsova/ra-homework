// a) 
let r=5;
const pi=3.14;
let area;
area=pi*(r**2);
// b)
console.log(`The area of the circle with radius ${r} and pi ${pi} = ${area}`);

// c)
let c =100;
// d)
radius=c/(2*pi);
// e)
d=2*radius;
// f)
console.log(`The circle has a circumference of ${c}. Its radius is ${radius} and its diameter is ${d}`);


